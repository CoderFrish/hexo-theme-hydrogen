main .articles {
  flex 1
  display flex
  flex-direction column
  gap var(--spacing-xl)
}

main .articles .article {
  border-radius var(--radius-md)
  background-color var(--bg-primary-color)
  border 1px solid var(--border-normal-color)
  transition all 150ms ease

  padding var(--spacing-md)

  &:hover {
    border-color var(--primary-normal-color)
    box-shadow var(--shadow-lg)
  }
}

main .articles .article a {
  text-decoration none
}

main .articles .article .meta {
  margin-bottom var(--spacing-sm);
}

main .articles .article .meta ul {
  list-style: none;
  display flex
  align-items center
  flex-direction row
  flex-wrap wrap
  gap var(--spacing-sm)
}

main .articles .article .meta ul li {
  color var(--text-muted-color)
  font-size var(--text-sm)

  display flex
  align-items center
  gap var(--spacing-xs)
}

main .articles .article .meta ul li a {
  @extend base-link
}

main .articles .article .excerpt {
  color var(--text-secondary-color)
  line-height 1.75
  margin-bottom var(--spacing-sm);
}

main .articles .article .cover {
  width 100%
  margin-bottom var(--spacing-xs)
  border-radius var(--radius-md)
  object-fit cover
}

main .articles .article .title {
  display block
  margin-bottom var(--spacing-sm);
  font-size var(--text-xl)
  line-height 1.25
  font-weight 600
  color var(--text-primary-color)
  transition all 150ms ease

  &:hover {
    color var(--primary-normal-color)
  }
}

main .articles .article .read {
  display inline-flex
  align-items center
  gap var(--spacing-xs)
  transition all 150ms ease
  color var(--primary-normal-color)
  font-size var(--text-sm)
  font-weight 500

  &:hover {
    gap var(--spacing-sm)
  }
}

main .articles .article .read iconify-icon {
  display inline-flex
  align-items center
}
